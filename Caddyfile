# CARE USG Studio reverse proxy — the studio itself, nothing else.
# (The v1-v3 optional /ohif same-origin viewer is gone: this app is
# standalone sonography reporting with no PACS viewer.)

:8090 {
	handle {
		reverse_proxy studio:3000
	}
}
